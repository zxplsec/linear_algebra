// pow.c: 
#include <stdio.h>
#include <math.h>
int main(void)
{
  printf("   2^3   = %f\n", pow(2.0,3.0));
  printf(" sqrt 2  = %f\n", pow(2.0,0.5));
  printf(" 3^(1/4) = %f\n", pow(3.0,0.25));
  return 0;
}
